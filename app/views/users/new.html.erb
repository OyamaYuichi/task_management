<div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <div class="content col-md-6">

<h1>新規登録</h1>
<% if @user.errors.any? %>
  <div class="center id="error_explanation">
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

    <%= form_with(model: @user, local: true) do |f| %>
      <p><%= f.label :name %></p>
      <p class="new-user"><%= f.text_field :name %></p>
      <p><%= f.label :email %></p>
      <p class="new-user"><%= f.email_field :email %></p>
      <p><%= f.label :password %></p>
      <p class="new-user"><%= f.password_field :password %></p>
      <p><%= f.label :password_confirmation %></p>
      <p class="new-user"><%= f.password_field :password_confirmation %></p>
      <p class="new-user"><%= f.submit "新規登録" %></p>
    <% end %>

</div>
    <div class="col-md-3"></div>
  </div>
  </div>
